package errs

/*
import (
	"bytes"
	"fmt"

	"github.com/tealeg/xlsx"
)

func NewRow(line int, text string, row *xlsx.Row) RowError {

}

func (my RowError) Error() string {
	var buf bytes.Buffer

	for _, cell := range my.Cells {
		buf.WriteString()
	}

	return
}
*/
